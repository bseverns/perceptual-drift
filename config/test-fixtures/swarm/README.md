# Swarm Scenario Fixtures

These fixtures are replay inputs for:

`software/swarm/replay_scenario.py`

Current scenarios:

- `scenario_dual_wave.yaml`: two-participant lateral tug with steady consent.
- `scenario_consent_handoff.yaml`: three-participant consent/energy handoff.

Use them for repeatable swarm rehearsal behavior checks and latency runs.

